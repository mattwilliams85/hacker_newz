<h1>Hacker News</h1>

<ul>
  <% @links.each do |link| %>
    <p><h4><%= link_to link.title, "http://#{link.url}"%></h4> <%= link_to "Edit", edit_link_path(link) %> | <%= link_to "Delete", link_path(link), :method => :delete %> </p>
  <% end %>
</ul>
